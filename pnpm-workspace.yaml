allowBuilds:
  esbuild: true

# Pin transitive `ws` to the fixed release for the uninitialized-memory
# disclosure in `websocket.close()` (GHSA-43c4-fjxq-4qpm). `ws` only
# enters the tree via `convex` and `convex-test` (dev), so this is
# defense-in-depth: GitHub auto-dismissed the alert because we don't
# exercise the affected close() path, but bumping costs nothing.
overrides:
  ws: ^8.20.1
